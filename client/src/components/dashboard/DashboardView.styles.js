import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
    DashboardWrapper: {
        borderRadius: '2px',
        fontSize: '14px',
        lineHeight: '14px',
    }
});

export default styles;
