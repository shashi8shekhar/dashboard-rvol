import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
    DashboardWrapper: {
        borderRadius: '2px',
        fontSize: '14px',
        lineHeight: '14px',
        minWidth: '100vw',
        minHeight: '100vh',
    }
});

export default styles;
