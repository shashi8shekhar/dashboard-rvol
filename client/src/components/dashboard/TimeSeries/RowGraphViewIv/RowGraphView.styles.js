import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
    RowGraphWrapper: {
    },
    TableGraphActions: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        padding: 10,
        background: '#FFF',
    }
});

export default styles;
